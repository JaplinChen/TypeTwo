# TypeTwo 產品化落地計劃（2026-05-26）

## 目標

把 TypeTwo 從「本機可跑、Docker 可驗證、CI 綠」推進到「公司內部可穩定使用、公司網路外可安全連線、管理員可維運、使用者不用理解工程細節」。

本計劃先以內部 Beta 為第一個產品化節點，不直接跳到 SaaS 或 multi-tenant。原因是目前已有 FastAPI 詞彙表後端、PostgreSQL、Docker Compose、Caddy HTTPS overlay、Flutter 詞彙同步 UI、admin/editor/user 角色與 smoke test；最短落地路徑是把這些能力補齊成可部署、可登入、可備份、可驗證的一套公司內部服務。

## 現況判斷

已具備：

- Flutter App 可設定詞彙表同步 URL、Email、密碼，並可登入、同步 approved 詞彙。
- FastAPI backend 已提供 `/auth/login`、`/glossary`、`/users`、pending approve/reject/history。
- Docker Compose 可啟動 `api` 與 PostgreSQL `db`。
- `docker-compose.prod.yml` 已有 Caddy reverse proxy、HTTPS、HSTS 基礎設定。
- 已有 `scripts/check_typetwo_prod_env.ps1` 檢查 production secret。
- 已有 `scripts/smoke_typetwo_glossary_api.ps1` 可實測登入、approved 詞彙包、user pending、admin approve，且預設會清理 smoke data。
- CI `windows-quality` 已成功跑過。

主要缺口：

- 外部可連線部署流程尚未產品化：domain、HTTPS、環境變數、備份、還原、升級、監控仍偏工程手動。
- 帳號生命週期不完整：沒有正式改密碼、重設密碼、首次登入強制改密碼、邀請或完整 user management UX。
- 安全基線不足：production runtime 尚未強制拒絕預設密碼、缺少 rate limit、CORS allowlist、request id、稽核查詢與登入事件記錄。
- App 雲端同步 UX 還像工程工具：使用者需要知道 URL、帳號、密碼、同步按鈕含義；錯誤訊息、token 過期、連線診斷、同步衝突都需要補強。
- 詞彙治理流程不足：pending 審核、匯入預覽、批次操作、版本回復與 audit history 尚未形成完整管理體驗。
- 發版流程未閉環：缺少 image tag、release artifact、migration gate、staging smoke、production smoke 與 rollback runbook。

## 產品化原則

1. 先交付單租戶公司內部版，不先做 multi-tenant。
2. 後端不直接裸露 `18000` 到 Internet；正式入口必須走 HTTPS reverse proxy。
3. production 不允許使用開發預設 secret、預設 admin password 或弱 JWT secret。
4. 所有會寫資料的 smoke test 必須 cleanup，cleanup 失敗就視為測試失敗。
5. 所有產品化功能都要有對應驗收命令或可人工驗收步驟。
6. 先把管理員流程做好，再優化一般使用者 onboarding。
7. 文件與腳本要能讓下一台機器重建同一套服務。

## 里程碑總覽

| 階段 | 目標 | 預估產出 |
| --- | --- | --- |
| M0 | 產品化基線與風險收斂 | production guard、部署文件、release checklist |
| M1 | 公司內部 Beta 可部署 | HTTPS domain、正式 `.env`、備份還原、smoke 驗證 |
| M2 | 帳號與管理 UX 可用 | user management UI、改密碼、停用、角色調整 |
| M3 | 同步 UX 產品化 | 連線測試、登入狀態、token 過期、同步診斷、衝突提示 |
| M4 | 詞彙治理可維運 | pending 管理、批次審核、匯入預覽、history 查詢 |
| M5 | 發版與維運閉環 | image tag、installer、migration gate、監控、rollback |

## M0：產品化基線與風險收斂

### 交付目標

建立「不可用開發設定上線」的硬防線，並把目前工程驗證整理成可重複執行的 checklist。

### 實作項目

- 後端新增 production mode 設定：
  - `ENVIRONMENT=development|production`
  - `PUBLIC_BASE_URL`
  - `CORS_ALLOWED_ORIGINS`
- production 啟動 guard：
  - `JWT_SECRET` 不可等於 `change-this-secret-before-production`、`dev-only-secret` 或短於 32 字元。
  - `POSTGRES_PASSWORD` 不可等於 `change-me`。
  - `ADMIN_PASSWORD` 不可等於 `change-me-now`、`password`、`admin`、`secret`，且長度至少 12。
  - production 禁止 `AUTO_CREATE_TABLES=true`，必須使用 Alembic migration。
- 補 `DEPLOYMENT.md`：
  - VPS / Windows Docker Desktop / Linux Docker Engine 三種部署路線的差異。
  - 正式建議路線：Linux VPS + Docker Compose + Caddy + PostgreSQL volume。
  - domain DNS、HTTPS、環境變數、啟動、migration、smoke test、備份、還原。
- 補 `RELEASE_CHECKLIST.md`：
  - 本機測試。
  - Docker build。
  - production env check。
  - staging smoke。
  - PR/CI 狀態。
  - 備份確認。
  - rollback 指令。
- 擴充 `scripts/check_typetwo_prod_env.ps1`：
  - 檢查 `ENVIRONMENT=production` 時必填 `GLOSSARY_DOMAIN`、`ACME_EMAIL`、`PUBLIC_BASE_URL`。
  - 檢查 `PUBLIC_BASE_URL` 必須是 `https://`。

### 驗收標準

- development 可以使用本機 Docker 預設值啟動。
- production 使用預設 secret 時啟動前檢查會失敗。
- production 文件能從空白 VPS 完成部署。
- `.\scripts\smoke_typetwo_glossary_api.ps1 -BaseUrl https://正式網域` 可通過。

### 建議測試

```powershell
python -m pytest backend\tests
python -m pytest src\tests
.\scripts\check_typetwo_prod_env.ps1
docker compose -f docker-compose.yml -f docker-compose.prod.yml config
```

## M1：公司內部 Beta 可部署

### 交付目標

讓公司網路外的使用者可以透過固定 HTTPS URL 登入並同步，不再依賴本機 `localhost`。

### 部署決策

建議正式 Beta 使用：

- Linux VPS 或公司 DMZ server。
- Docker Compose 管理 backend、PostgreSQL、Caddy。
- domain 例如 `https://typetwo-glossary.company.com`。
- App 端同步 URL 填正式 domain，不填 `localhost`。

不建議正式使用：

- 直接把 FastAPI `18000` port forward 到 Internet。
- 長期依賴 ngrok free tunnel。
- 在員工個人電腦上承擔正式服務。

### 實作項目

- 補 `.env.example.production`：
  - `ENVIRONMENT=production`
  - `POSTGRES_PASSWORD`
  - `JWT_SECRET`
  - `ADMIN_EMAIL`
  - `ADMIN_PASSWORD`
  - `GLOSSARY_DOMAIN`
  - `ACME_EMAIL`
  - `PUBLIC_BASE_URL`
  - `AUTO_CREATE_TABLES=false`
- 建立 production migration flow：
  - `docker compose up -d db`
  - `docker compose run --rm -e AUTO_CREATE_TABLES=false api alembic upgrade head`
  - `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d`
- 補 staging smoke：
  - 用 staging domain 跑 smoke。
  - cleanup 後查 smoke key 數量為 0。
  - 查 smoke user 全部 inactive。
- 備份還原演練：
  - 每日 `pg_dump`。
  - zip 檔保留天數。
  - 還原到臨時 volume 驗證。
- 基礎監控：
  - `docker compose ps`
  - `/health`
  - disk usage。
  - backup success log。

### 驗收標準

- 外部網路可連 `https://typetwo-glossary.company.com/health`。
- App 可用正式 domain 登入。
- admin 可建立 user。
- user 可提交 pending 詞彙。
- admin/editor 可 approve。
- approved 詞彙可同步回 App。
- 備份可還原到新 DB volume。

## M2：帳號與管理 UX 可用

### 交付目標

管理員不用碰 API 或資料庫，就能建立、停用、重設使用者與調整角色。

### 後端實作項目

- 新增目前登入者 API：
  - `GET /auth/me`
- 新增改密碼 API：
  - `POST /auth/change-password`
  - 需要舊密碼。
- 新增 admin 重設密碼 API：
  - `POST /users/{id}/reset-password`
  - 回傳一次性 temporary password 或設定 `mustChangePassword=true`。
- User model 增加：
  - `must_change_password`
  - `last_login_at`
  - `created_at`
  - `updated_at`
- 登入成功更新 `last_login_at`。
- 停用帳號後既有 token 應失效：目前 `current_user` 會查 DB 並檢查 `is_active`，這點要保留測試。
- 補登入失敗與弱密碼測試。

### Flutter 實作項目

- 使用者管理 dialog 升級：
  - 顯示 email、role、active、last login。
  - 新增 user。
  - 停用/啟用 user。
  - 角色下拉選單。
  - reset password。
- 首次登入或 `mustChangePassword` 時顯示改密碼流程。
- 登入後顯示目前帳號與角色。
- 登出時清 token，但保留 URL/email。

### 驗收標準

- admin 可在 App 內建立 user/editor/admin。
- 新 user 可登入。
- 停用 user 後無法登入，既有 token 也無法繼續呼叫 API。
- admin 可重設密碼。
- user 可自行改密碼。
- 不存在的 email 或錯誤密碼有清楚錯誤訊息。

## M3：同步 UX 產品化

### 交付目標

使用者能理解「目前是否連到雲端、是否已登入、同步是否成功、失敗要怎麼處理」。

### Flutter 實作項目

- 雲端同步面板拆成狀態區與操作區：
  - URL。
  - 連線測試。
  - 登入/登出。
  - 目前帳號。
  - 角色。
  - 上次同步時間。
  - pending local changes 數量。
  - 遠端 pending review 數量。
- 增加「測試連線」按鈕：
  - 呼叫 `/health`。
  - 顯示 API reachable / DB ok / domain error / TLS error / timeout。
- Token 過期或 401：
  - 顯示重新登入。
  - 不要靜默失敗。
- 同步錯誤分類：
  - URL 空白。
  - 網路不通。
  - 帳密錯誤。
  - 權限不足。
  - server 500。
  - JSON 格式錯誤。
- 同步按鈕狀態：
  - disabled when URL empty or not logged in。
  - loading spinner。
  - 成功後顯示同步筆數。
- pending local changes 顯示：
  - 顯示將同步哪些新增/修改/刪除。
  - 允許重試。
  - 允許清除本機 pending queue，但需確認。

### 後端實作項目

- `/health` 可選擇回傳版本：
  - app name。
  - version。
  - db status。
  - migration revision。
- API error response 統一格式：
  - `code`
  - `message`
  - `details`
- 加 request id header：
  - 方便使用者截圖回報時追查 log。

### 驗收標準

- 使用者第一次設定 URL 時可以先測連線。
- 登入錯誤與網路錯誤可被區分。
- token 過期後 UI 提示重新登入。
- 同步成功會更新上次同步時間。
- 離線新增詞彙後，恢復連線可同步 pending changes。

## M4：詞彙治理可維運

### 交付目標

讓 editor/admin 可以真正管理公司詞彙，不只是單筆 approve/reject。

### 實作項目

- Pending review UI：
  - 搜尋。
  - 依建立者、日期、語言對、狀態篩選。
  - 批次 approve。
  - 批次 reject。
  - reject reason。
- 詞彙 history UI：
  - 每個詞彙可查看建立、更新、approve、reject、delete 記錄。
  - 顯示 changed by 與 changed at。
- 匯入預覽：
  - TSV/JSON 上傳後先預覽。
  - 顯示新增、更新、衝突、錯誤列。
  - 使用者確認後才寫入。
- 匯出：
  - 匯出 approved。
  - 匯出 all statuses。
  - 匯出 pending。
- 版本回復：
  - 最小可先做單詞 history restore。
  - 大量回復等下一階段。
- 重複詞彙治理：
  - active term unique 已有後端基礎，UI 要提示同 context/source duplicate。
  - 匯入衝突需要選擇 keep existing / overwrite。

### 驗收標準

- editor 可以處理 pending review，但不能管理 users。
- admin 可以管理 users 與所有詞彙。
- user 提交詞彙一定進 pending，不會直接變 approved。
- 批次 approve 後 App 同步可取得新詞彙。
- 匯入錯誤列不會造成部分未知狀態污染。

## M5：發版與維運閉環

### 交付目標

每次發版都能回答：發了什麼、如何驗證、如何部署、如何回退、資料如何保護。

### CI/CD 實作項目

- Backend image tag：
  - `typetwo-glossary-api:<git-sha>`
  - `typetwo-glossary-api:<semver>`
  - `latest` 只供 development。
- GitHub Actions 增加：
  - backend tests。
  - Flutter analyze/test。
  - Docker build。
  - migration dry run。
  - smoke script 可在 service container 上跑。
- Release job：
  - Windows portable package 或 installer。
  - checksum。
  - release notes。
  - backend image artifact 或 registry push。
- Migration gate：
  - production deploy 前必須先備份。
  - migration 成功後才切換服務。
- Rollback runbook：
  - app rollback。
  - backend image rollback。
  - DB restore 條件與流程。

### 維運項目

- 日誌：
  - access log。
  - auth log。
  - glossary mutation audit log。
  - request id。
- 監控：
  - `/health`。
  - container restart count。
  - DB disk usage。
  - backup success/failure。
- 安全：
  - rate limit `/auth/login`。
  - CORS allowlist。
  - HTTPS only。
  - secret rotation 文件。
  - admin password rotation 文件。

### 驗收標準

- 任一 release 可以從 tag 找到對應 backend image、Flutter build 與 release notes。
- staging smoke 通過後才允許 production deploy。
- production deploy 前有備份。
- rollback 文件經過一次演練。

## 優先工作清單

### 第一週：建立安全部署底座

1. 新增 production mode 與 startup guard。
2. 擴充 `check_typetwo_prod_env.ps1`。
3. 新增 `.env.example.production`。
4. 新增 `DEPLOYMENT.md`。
5. 新增 `RELEASE_CHECKLIST.md`。
6. 在 staging 或本機 prod compose 跑完整 smoke。

完成定義：

- production 預設密碼會被擋。
- 正式部署文件能照做。
- Caddy HTTPS path 清楚。
- smoke test 可對指定 URL 執行。

### 第二週：帳號管理與登入體驗

1. 新增 `/auth/me`。
2. 新增 change password。
3. 新增 reset password。
4. User model 增加 `must_change_password`、`last_login_at`。
5. Flutter 使用者管理 UI 補齊。
6. Flutter 登入狀態與錯誤訊息整理。

完成定義：

- admin 不用 API 就能管理帳號。
- user 可以改密碼。
- 停用帳號立即失效。
- 錯誤訊息能讓使用者知道要改 URL、帳密或找管理員。

### 第三週：同步 UX 與詞彙審核

1. 增加「測試連線」。
2. 同步狀態面板產品化。
3. pending review UI 增加搜尋與批次操作。
4. history UI。
5. 匯入預覽設計與第一版實作。

完成定義：

- 使用者可以自行完成 URL 設定、登入、同步、排錯。
- editor 可以處理 pending 詞彙。
- 匯入前可以看到影響範圍。

### 第四週：發版、監控、備份還原

1. Docker image tag 版本化。
2. CI 增加 Docker build 與 smoke。
3. release notes 與 artifact。
4. 備份排程與還原演練。
5. production runbook。

完成定義：

- 每版可追溯。
- deploy 前有備份。
- staging smoke 通過。
- rollback 可演練。

## 驗收矩陣

| 類別 | 驗收項 | 方法 |
| --- | --- | --- |
| 部署 | 外部 HTTPS 可連 | 瀏覽器與 `Invoke-RestMethod https://domain/health` |
| 登入 | admin/user/editor 可登入 | Flutter UI 與 `/auth/login` |
| 權限 | user 只能送 pending | user `POST /glossary` 後 status 必為 pending |
| 權限 | editor 可 approve 但不可管 users | API/UI 測試 |
| 權限 | admin 可管 users | API/UI 測試 |
| 同步 | App 可拉 approved 詞彙 | Flutter 同步後本機 glossary 更新 |
| 離線 | pending queue 可重試 | 斷網新增、恢復網路同步 |
| 安全 | production 預設 secret 被擋 | `check_typetwo_prod_env.ps1` 與 startup guard |
| 資料 | 備份可還原 | restore 到臨時 volume |
| 發版 | CI 綠且 smoke 通過 | GitHub Actions 與 smoke script |

## 風險與處理

| 風險 | 影響 | 對策 |
| --- | --- | --- |
| 使用者把 `localhost` 當外部 URL | 公司外無法登入 | UI 與文件明確說明，新增連線測試 |
| production 使用預設密碼 | 高資安風險 | startup guard 與 env check |
| 直接暴露 `18000` | TLS、攻擊面、維運風險 | 正式只走 Caddy HTTPS |
| token 過期造成同步失敗 | 使用者以為資料遺失 | 401 顯示重新登入 |
| smoke test 污染 DB | 詞彙表混入測試資料 | cleanup 失敗即失敗，二次查詢驗證 |
| migration 破壞資料 | 詞彙資料損失 | deploy 前備份，staging restore 演練 |
| 帳號管理靠 API | 管理成本高 | M2 補完整 UI |

## 暫不做事項

- 不先做 multi-tenant。
- 不先做公開註冊。
- 不先做 billing。
- 不先做 Kubernetes。
- 不先做完整 SSO；若公司需要，可在內部 Beta 穩定後評估 OIDC。

## 進度紀錄

### 2026-05-29：M3 同步 UX 產品化增量

- TypeTwo Server「測試連線」已解析 `/health` 回應，成功時顯示 version、environment、migration revision；DB 異常與無效 JSON 會回傳可辨識錯誤。
- 同步時若遠端回 401「登入已失效」，App 會清除 token 與 role，讓 UI 回到需重新登入狀態，同時保留 `glossaryPendingChanges`，避免本機待同步變更遺失。
- 詞彙表精簡同步列與雲端同步頁已在 TypeTwo Server 需要登入時顯示「需要登入」，並停用同步按鈕，避免 token 過期後重複觸發失敗同步。
- 已新增對應 Flutter service/provider 測試。
- 已驗證：`flutter analyze`、`flutter test`、`py -3.12 -m pytest backend\tests`、`py -3.12 -m pytest src\tests`。

### 2026-05-29：M4 pending review 管理增量

- 雲端同步頁的 pending review dialog 已支援搜尋待審核詞彙，可依原文、譯文與 context key 篩選。
- pending review 已支援勾選、選取目前篩選結果、清除選取、批次核准與批次退回。
- Flutter provider 已新增 `approveGlossaryTerms()` 與 `rejectGlossaryTerms()`；批次核准會逐筆呼叫現有後端 approve endpoint，最後只同步一次 approved 詞彙。
- 已新增 pending review widget 測試與 provider HTTP 行為測試。
- 已驗證：`flutter analyze`、`flutter test`、`py -3.12 -m pytest backend\tests`、`py -3.12 -m pytest src\tests`。

### 2026-05-29：M4 reject reason 增量

- 後端 `POST /glossary/{id}/reject` 已可接收 `reason`，並將退回原因寫入 `glossary_term_history.reason`。
- 已新增 Alembic migration `202605290001_add_glossary_reject_reason.py`，讓 production DB 可升級 history schema。
- `/glossary/{id}/history` 回應已包含 `reason`，可供後續 history UI 顯示。
- Flutter 單筆退回與批次退回前會要求輸入退回原因，並透過 `GlossaryRemoteService.rejectTerm()` 傳到後端。
- 已新增後端 API 測試、Flutter remote service 測試與 pending review widget 測試。
- 已驗證：`flutter analyze`、`flutter test`、`py -3.12 -m pytest backend\tests`、`py -3.12 -m pytest src\tests`。

### 2026-05-29：M4 history UI 增量

- Flutter 已新增 `GlossaryRemoteTermHistory` model、`GlossaryRemoteService.listTermHistory()` 與 `ConfigProvider.listGlossaryTermHistory()`。
- 雲端同步頁 pending review 每筆詞彙已提供「紀錄」入口，可查看 operation、status、version、context、異動時間與退回原因。
- 已新增 Flutter remote service 測試與 pending review history widget 測試，驗證 history reason 可顯示。
- 已驗證：`flutter analyze`、`flutter test`、`py -3.12 -m pytest backend\tests`、`py -3.12 -m pytest src\tests`。

### 2026-05-29：M4 匯入預覽後端契約增量

- 後端已新增 `POST /glossary/import/preview`，使用既有 `GlossaryImportRequest` payload，且不寫入資料庫。
- 匯入預覽會逐筆回傳 `imported`、`updated`、`unchanged`、`skipped` 判定，並回傳總計數；空白原文會標示 `skipped` 與原因。
- 已新增 `GlossaryImportPreviewItem` 與 `GlossaryImportPreviewResponse` schema，作為後續 Flutter 匯入預覽 UI 的資料契約。
- 已新增 service/API 測試，驗證 preview 統計、逐筆 action、既有詞目前值，以及 preview 不會污染資料。
- `backend/README.md` 已補上 `/glossary/import/preview` 說明。
- 已驗證：`py -3.12 -m pytest backend\tests`。

### 2026-05-29：M4 匯入預覽 Flutter 增量

- Flutter 已新增 `GlossaryImportPayload`、`GlossaryImportPreviewResult`、`GlossaryImportPreviewItem` 與 `GlossaryImportResult`，可解析 AppConfig JSON、扁平 JSON 與後端 preview 回應。
- `GlossaryRemoteService` 已新增 `previewImport()` 與 `importGlossary()`，`ConfigProvider` 已新增 `previewGlossaryImport()` 與 `importGlossaryRemote()`；正式匯入完成後會重新同步 approved 詞彙與 remote ids。
- 雲端同步頁 TypeTwo Server editor/admin 已新增「匯入預覽」入口，選擇 JSON 後先顯示新增、更新、未變更、略過統計與逐筆影響，使用者確認後才正式寫入。
- 已新增 Flutter remote service、provider HTTP 行為與雲端同步頁 widget 測試，驗證 preview/import contract、正式匯入後同步，以及 editor 可見入口。
- 已驗證：`dart format`、`flutter analyze`、`flutter test test/services/glossary_remote_client_test.dart`、`flutter test test/services/glossary_sync_service_test.dart`、`flutter test test/screens/cloud_sync_tab_test.dart`。

### 2026-05-29：M4 history restore 增量

- 後端已新增 `POST /glossary/{id}/history/{history_id}/restore`，可將單筆詞彙回復到指定 history snapshot，並新增 `restore` history 紀錄。
- restore 會沿用既有 active duplicate 檢查，避免回復後造成同 context/source 衝突。
- Flutter 已新增 `GlossaryRemoteService.restoreTermHistory()` 與 `ConfigProvider.restoreGlossaryTermHistory()`；雲端同步頁 history dialog 每筆紀錄提供「回復」操作，完成後重新同步 approved 詞彙。
- `backend/README.md` 已補上 history restore API。
- 已驗證：`py -3.12 -m pytest backend\tests\test_glossary_service.py backend\tests\test_glossary_api.py`、`flutter test test/services/glossary_remote_client_test.dart`、`flutter test test/services/glossary_sync_service_test.dart`、`flutter test test/screens/cloud_sync_tab_test.dart`。

### 2026-05-29：M5 發版與維運閉環增量

- 已新增 `scripts/check_backend_migrations.ps1` migration gate，檢查 Alembic 單一 head；若 `DATABASE_URL` 已設定，會對該資料庫執行 `alembic upgrade head` 並確認目前 revision 到 head。
- GitHub Actions `CI` 已新增 `backend-quality` job：安裝 backend dependencies、執行 backend pytest、以 PostgreSQL service 跑 migration gate、建置 backend Docker image，並用 `smoke_typetwo_glossary_api.ps1` 做 TypeTwo Server smoke 與 cleanup 驗證。
- GitHub Actions `Release Windows Installer` 已新增 `backend-image-artifact` job，為 backend image 建立 git SHA 與 release version tag，並輸出 `typetwo-glossary-api-<version>.tar` release artifact。
- 已新增 `DEPLOYMENT.md`、`RELEASE_CHECKLIST.md` 與 `ROLLBACK_RUNBOOK.md`，補齊部署 gate、staging/production smoke、release artifact、DB 備份與回退步驟。
- `README.md` 已更新連到 deployment、release checklist 與 rollback runbook。
- 已驗證：`.\scripts\check_backend_migrations.ps1 -Python py -PythonArgs -3.12`、`docker compose build api`、`docker compose up -d --build` + `.\scripts\smoke_typetwo_glossary_api.ps1` + `docker compose down -v`。

### 2026-05-29：M1/M5 備份還原驗證增量

- 已新增 `.env.example.production`，列出 production 必填環境變數與安全預設說明，避免直接複用 development secret。
- 已新增 `scripts/verify_typetwo_postgres_backup.ps1`，可將 `backup_typetwo_postgres.ps1` 產出的 `.sql.zip` 還原到獨立 compose project 與臨時 volume，並確認 `users`、`glossary_terms`、`glossary_term_history` 三張必要資料表存在。
- `DEPLOYMENT.md` 已補上 `.env.example.production` 使用方式、備份、直接還原與臨時 volume 還原驗證流程。
- `RELEASE_CHECKLIST.md` 與 `ROLLBACK_RUNBOOK.md` 已補上備份還原驗證 gate，讓 deploy/rollback 前的 DB 備份不只「有檔案」，還能證明可還原。
- 已驗證：PowerShell parser 檢查所有維運腳本、`docker compose up -d --build` + `.\scripts\smoke_typetwo_glossary_api.ps1`、`.\scripts\backup_typetwo_postgres.ps1 -OutputDir .\backups-test -KeepDays 1`、`.\scripts\verify_typetwo_postgres_backup.ps1 -BackupZip <backup.zip>`、`docker compose down -v`。

### 2026-05-29：M4 匯出與匯入衝突策略增量

- 後端 `GlossaryImportRequest` 已新增 `conflictStrategy`，支援 `overwrite` 與 `keepExisting`；`keepExisting` 匯入時會保留既有詞彙，不覆蓋 target/status。
- 匯入預覽在 `keepExisting` 模式下，既有且不同內容的詞彙會標示 `skipped` 並顯示「已保留既有詞彙」，讓使用者能先看到衝突處理結果。
- Flutter 遠端匯入流程已在預覽前要求選擇「覆蓋既有詞彙」或「保留既有詞彙」，並將策略送到後端 preview/import。
- 雲端同步頁 editor/admin 已新增遠端匯出入口，可匯出 approved、pending、rejected 或全部狀態為 JSON，包含 `glossary`、`langGlossary` 與逐筆 `terms`。
- 已新增後端 service/API 測試、Flutter remote payload 測試與雲端同步頁入口測試。
- 已驗證：`py -3.12 -m pytest backend\tests\test_glossary_service.py backend\tests\test_glossary_api.py`、`flutter analyze`、`flutter test test/services/glossary_remote_client_test.dart`、`flutter test test/screens/cloud_sync_tab_test.dart`。

### 2026-05-29：M5 release checksum 與完成度審核增量

- GitHub Actions release workflow 已改名為 `Release TypeTwo Artifacts`，並為 Windows installer 與 backend image tar 產出 SHA256 checksum。
- release workflow 會上傳並附加 `setup_typetwo.exe.sha256` 與 `typetwo-glossary-api-<version>.tar.sha256`，讓每次 release artifact 可驗證完整性。
- `RELEASE_CHECKLIST.md` 已補上 installer/backend image checksum 欄位與 release artifact 驗收項。
- 已新增 `COMPLETION_AUDIT.md`，明確區分 M0-M5 本機已完成證據與仍需外部環境驗證的項目。
- `README.md` 已新增完成度審核入口，讓後續接手者可直接看到目前不能本機宣告完成的外部 gate。

### 2026-05-29：GitHub 外部 gate 驗證增量

- 已建立分支 `codex/productize-typetwo-m0-m5` 與 draft PR #44。
- GitHub Actions PR CI 已通過：`backend-quality` 與 `windows-quality` 皆為 success。
- 已手動觸發 release workflow_dispatch run `26616184411`，`backend-image-artifact` 與 `build-windows-installer` 皆為 success。
- 已確認 workflow artifacts 包含 `setup_typetwo.exe`、`setup_typetwo.exe.sha256`、backend image tar 與 backend image tar `.sha256`。
- 已下載 workflow artifacts 並重新計算 SHA256，確認 installer 與 backend image checksum 皆吻合。
- PR #44 已合併到 `master`，`master` CI 已通過。
- 已建立並合併版本更新 PR #45，將 installer、Flutter app version、pubspec 與版本測試同步到 `1.0.18`。
- 已建立 GitHub Release `v1.0.18`，release workflow run `26617247786` 已通過，並成功附加 installer、backend image tar 與各自 SHA256。
- 已下載 GitHub Release `v1.0.18` assets 並重新計算 SHA256，確認 release 內附 checksum 皆吻合。

### 2026-05-29：部署 evidence gate 增量

- 已新增 `scripts/test_typetwo_deployment_gate.ps1`，可對 staging/production URL 統一執行 `/health`、smoke、備份還原驗證，並輸出 JSON evidence。
- 已新增 GitHub Actions `Deployment Gate` workflow，可手動輸入 staging/production URL，使用 environment secrets 執行遠端 smoke 並上傳 evidence artifact。
- `DEPLOYMENT.md`、`RELEASE_CHECKLIST.md` 與 `COMPLETION_AUDIT.md` 已補上 deployment gate 使用方式與完成條件。
- 已用本機 Docker 實測 deployment gate，`health` 與 `smoke` 皆通過，並確認 cleanup 成功。
- repo 目前沒有 staging/production 主機、DNS、GitHub Secrets 或 Variables；最後外部 gate 仍需真實 HTTPS URL 與 admin credentials 才能執行。

### 2026-05-29：production env 與 release artifact 驗證腳本增量

- 已新增 `scripts/new_typetwo_prod_env.ps1`，可輸入 domain、ACME email、admin email 後產生強 secret `.env.production`，並自動執行 production env guard。
- 已新增 `scripts/test_typetwo_release_artifacts.ps1`，可下載指定 GitHub Release 的 installer、backend image tar 與 SHA256 檔，並重新計算 checksum。
- `DEPLOYMENT.md`、`RELEASE_CHECKLIST.md` 與 `COMPLETION_AUDIT.md` 已補上 production env 產生與 release artifact 驗證命令。
- 已實測 production env 產生腳本與 release artifact 驗證腳本，兩者皆通過。

## 下一步建議

下一輪優先補齊外部環境證據：

1. 建立 staging/production 主機、DNS 與 HTTPS domain。
2. 在 staging 真機跑一次 `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build` 與 production-like smoke。
3. production deploy 前先完成 production DB 備份，並用非 production volume 還原驗證。
4. 若要宣告 M0-M5 全部完成，需補齊外部 domain/HTTPS、staging/production smoke、production 備份還原與 rollback 演練證據。
